# Emulator UI Skin Engine

This UI layer provides a modular device-skin system for web-based emulators.

- Drop-in: does not modify emulator cores  
- Files: index.html, styles.css, engine.js, skins.js, api.md  
- Exposes `window.EmuUI` API to integrate with emulator core

See `api.md` for core contract.
